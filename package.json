{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "ken-explorer/build": "echo START && lerna bootstrap && echo BOOTSTRAPPEDD && lerna run netlify-build"
  }
}