{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "ken-explorer/build": "echo START && lerna bootstrap && echo BOOTSTRAPPED && lerna run netlify-build"
  }
}