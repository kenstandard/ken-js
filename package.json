{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "ken-explorer/build": "echo START && lerna bootstrap && echo BOOTSTRAPPEDD && cd packages/ken-js && npm install && npm run build && cd ../.. && lerna run netlify-build"
  }
}