{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "ken-js/build": "cd packages/ken-js && npm install && npm run build && cd ../..",
    "ken-explorer/build": "echo [Begin Build] && npm bootstrap && echo [Bootstrap Complete] && npm ken-js/build && echo [ken-js-build complete] && lerna run netlify-build"
  }
}