{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "ken-js/build": "cd packages/ken-js && npm install && npm run build && cd ../..",
    "ken-explorer/buildn": "cd packages/ken-explorer && npm run build && cd ../..",
    "ken-explorer/build": "echo [Begin Build] && npm run bootstrap && echo [Bootstrap Complete] && npm run ken-explorer/buildn"
  }
}